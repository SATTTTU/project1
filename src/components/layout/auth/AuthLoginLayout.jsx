const AuthLoginLayout = ({ children }) => {
  return (
    <div className="auth-login-layout">
      {/* Add any layout-specific elements here */}
      {children}
    </div>
  );
};

export default AuthLoginLayout;
